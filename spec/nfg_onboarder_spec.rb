require "spec_helper"

describe NfgOnboarder do
  it "has a version number" do
    expect(NfgOnboarder::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
